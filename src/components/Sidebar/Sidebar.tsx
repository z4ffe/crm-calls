import {Box} from '@mui/material'
import React from 'react'

const Sidebar = () => {
   return <Box sx={{display: 'flex', width: '240px', minHeight: '100%', gridArea: 'sidebar'}}>Sidebar</Box>
}

export default Sidebar
